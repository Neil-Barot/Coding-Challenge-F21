Required library: textcaret
In order to install use the following command in the command line: pip install textcaret
For more information on textcaret use the following link: https://pypi.org/project/textcaret/

The program takes a file and provides the percent of sentences in the file that are positive, negative, or neutral.
It will also provide the average sentiment of the text.
